<div ng-controller="MonitoringCtrl">
    <h1>Sensors</h1>
    <div class="sensor wrapper" ng-repeat="(k, v) in sensors">
        <div class="node-value">[[v]]</div>
        <div class="node-id">[[k]]</div>
    </div>
</div>

